\documentclass[aps,onecolumn,nofootinbib,pra]{article}

\usepackage{../../spec_files/arxiv}
\input{../../spec_files/standard_preamble.tex}

\input{../../macros/organization_macros.tex}
\input{../../macros/general_macros.tex}
\input{../../macros/tc_macros.tex}
\input{../../macros/tikz_macros.tex}

%% Specific for Causal

\newcommand{\doof}[1]{\mathrm{do}\left(#1\right)}
\newcommand{\dovariable}{D}
\newcommand{\dovariableof}[1]{\dovariable_{#1}}

\newcommand{\doindex}{d}
\newcommand{\doindexof}[1]{\doindex_{#1}}

\newcommand{\indexeddovariableof}[1]{\dovariableof{#1}=\doindexof{#1}}

\newcommand{\causalsymbol}{c}

\begin{document}
    \title{Climbing the latter of causation with \tnreason{}}

    \maketitle
    \date{\today}

    \begin{abstract}
        We here describe how to represent Causal AI models in the \tnreason{} formalism.
    \end{abstract}

    We find tensor network models for the different rungs of the \emph{latter of causation} \cite{pearl_book_2018}
    \begin{itemize}
        \item \textbf{Rung 1: Association} -- captured by Bayesian Networks
        \item \textbf{Rung 2: Intervention} -- captured by do-calculus, for which we introduce intervention variables
        \item \textbf{Rung 3: Counterfactuals} -- captured by functional causal models (see \cite[Chapter~21]{koller_probabilistic_2009}) involving response variables
    \end{itemize}

    Following the formalism of Chapter~21 in \cite{koller_probabilistic_2009}:
    \begin{itemize}
        \item Mutilation of the Bayesian network is captured by contractions with one-hot encodings to do-variables $\dovariableof{\catenumerator}$.
        \item Intervention queries are formalized by contractions with one-hot encodings to all do-variables
        \item Response variables are formalized by selection variables
        \item Counterfactual queries are modeled as intervention queries on counterfactual twin networks (see \cite[Chapter~21]{koller_probabilistic_2009})
    \end{itemize}


    \input{sections/association}
    \input{sections/intervention}
    \input{sections/counterfactuals}


    \section{Learning}

    Given a parametrization of hypothesis distribution by the states of selection variables, we can include the intervention variables.
    The cores to be trivialized by intervention can depend on the state of the selection variables.
    For example, a selection variable might select whether the cores direction is from $\catvariableof{0}$ to $\catvariableof{1}$ or in the other direction.
    In both cases only one of the to intervention variables $\dovariableof{0}$ and $\dovariableof{1}$ influences the core.

    \bibliographystyle{plainnat}
    \bibliography{../../references.bib}

\end{document}