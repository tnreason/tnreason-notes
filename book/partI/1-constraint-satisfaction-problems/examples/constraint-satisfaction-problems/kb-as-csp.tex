\begin{example}{Knowledge base satisfaction as a constraint satisfaction problem}
    Given a knowledge base $\kb=\{\formulaof{\selindex}\wcols\selindexin\}$ deciding whether the knowledge base is satisfiable is a constraint satisfaction problem.
    Here the by the formula $\formulaof{\selindex}$ affected variables $\catvariableof{\edgeof{\selindex}}$ build the hyperedges, which are decorated by the constraint tensors coinciding with the formulas $\formulaat{\catvariableof{\edgeof{\selindex}}}$.
    %% More detailed example here?
\end{example}