\documentclass{beamer}

\input{../../spec_files/course_preamble.tex}
\input{../../spec_files/course_macros.tex}

\setchatwotitledate{
    Causality
}{
    June 29, 2026
}
\begin{document}

    {\frame[plain]{\titlepage}}
    \showoutlineatsection


    \section{Bayesian network hypergraphs}

    \begin{frame}{Example: Random walk}
        \begin{minipage}{0.6\linewidth}
            Imagine a random walk through circular cells:
            \begin{itemize}
                \item $\catvariableof{\catenumerator}$ describes at timestep $\catenumeratorin$ the position
                \item With probability $0.5$ we stay at a cell, and to each neighboring cell with $0.25$
            \end{itemize}
        \end{minipage}
        \begin{minipage}{0.35\linewidth}
            \stantikzpic{

                \draw (0,0) circle (2);
                \draw (0,0) circle (4);

                \foreach \angle in {0, 45, ..., 315} {
                    \draw (\angle:2) -- (\angle:4);
                }
            }
        \end{minipage}


        This is a Markov chain with the transition matrix:
        \begin{align*}
            \condprobat{\catvariableof{\catenumerator}}{\catvariableof{\catenumerator-1}} =
            \begingroup
            \tiny
            \coloredmatrixof{
                0.50 & 0.25 & 0 & 0 & 0 & 0 & 0 & 0.25 \\
                0.25 & 0.50 & 0.25 & 0 & 0 & 0 & 0 & 0 \\
                0 & 0.25 & 0.50 & 0.25 & 0 & 0 & 0 & 0 \\
                0 & 0 & 0.25 & 0.50 & 0.25 & 0 & 0 & 0 \\
                0 & 0 & 0 & 0.25 & 0.50 & 0.25 & 0 & 0 \\
                0 & 0 & 0 & 0 & 0.25 & 0.50 & 0.25 & 0 \\
                0 & 0 & 0 & 0 & 0 & 0.25 & 0.50 & 0.25 \\
                0.25 & 0 & 0 & 0 & 0 & 0 & 0.25 & 0.50
            }{\catvariableof{\catenumerator},\catvariableof{\catenumerator-1}}
            \endgroup
        \end{align*}
    \end{frame}

    \begin{frame}{How to distinguish the past from the present?}

        We have two decomposition schemes as Bayesian networks, both decorated with the same transition matrices:

        \input{tikz/markov-chain}

    \end{frame}


    \begin{frame}{The BN hypergraph is not unique}

%        Imagine a Markov chain.
%            {\bf Example:} Markov chain. Does the past cause the future or the future the past?

        We can manipulate the hypergraph, and still find a Bayesian network representation of a distribution.

        Causation is not (always) represented in the directionality!

            {\bf Idea:} Distinguish by interventions.


    \end{frame}


    \begin{frame}{The rungs of causation}
        \stantikzpic{
            \drawtextnode{0}{0}{\includegraphics[width=6cm]{img/rungs}}

            \draw [->] (9,6) -- (11,6);
            \node[anchor=west] at (11,6)    {{\bf Response variables}};
            \draw [->] (9,0.5) -- (11,0.5);
            \node[anchor=west] at (11,0.5)  {{\bf Do variables}};
            \draw [->] (9,-5) -- (11,-5);
            \node[anchor=west] at (11,-5)   {{\bf Bayesian Networks}};
        }

    \end{frame}


    \begin{frame}{Probability distributions: Associations}

        Contractions of distributions capture associations in the data:
        \begin{itemize}
            \item Marginal distributions
            \item Conditional distributions
        \end{itemize}
%        \begin{align*}
%            \probat{}
%        \end{align*}

        {\bf Limitation:} Do not model causations.
    \end{frame}


    \section{Intervention}


    \begin{frame}{Interventions}

        This appears in important situation:
        \begin{itemize}
            \item Will product sales increase if we lower the price?
            \item Will I pass the exam tomorrow if I study today?
        \end{itemize}

                \begin{center}
            \bf Associations are not enough for these questions!
        \end{center}
    \end{frame}

    \begin{frame}{Do calculus: Modelling interventions}

        Add further variables $\dovariableof{\catenumerator}$ representing interventions:

        \begin{itemize}
            \item Intervention on the variable $\catvariableof{\catenumerator}$ when $\doindexof{\catenumerator}\in[\catdimof{\catenumerator}]$
            \item No intervention on the variable when $\doindexof{\catenumerator}=\catdimof{\catenumerator}$
        \end{itemize}

        This is realized by a tensor
        \begin{align*}
            \condprobat{\catvariableof{\catenumerator}}{\catvariableof{\parentsof{\catenumerator}},\dovariableof{\catenumerator}}
            &= \sum_{\catindexofin{\catenumerator}} \onehotmapofat{\catindexof{\catenumerator}}{\catvariableof{\catenumerator}}
            \otimes \onesat{\catvariableof{\parentsof{\catenumerator}}}
            \otimes  \onehotmapofat{\catindexof{\catenumerator}}{\dovariableof{\catenumerator}} \quad \text{(intervention)} \\
            & \quad + \condprobat{\catvariableof{\catenumerator}}{\catvariableof{\parentsof{\catenumerator}}}
            \otimes \onehotmapofat{\catdimof{\catenumerator}}{\dovariableof{\catenumerator}} \quad \text{(no intervention)}
        \end{align*}

        This resolves the ambiguity in Bayesian network representation!

    \end{frame}

    \begin{frame}{Example: Random walk}


        Random walk example with $\catdim=3$:
        \stantikzpic{
            \drawtextnode{-7}{0}{$\probat{\catvariableof{\catenumerator}|\catvariableof{\catenumerator-1},\dovariableof{\catenumerator}}\, = $}
            \drawtextnode{0}{0}{
                \tiny
                $\begin{pmatrix}
                     1 & 1 & 1 \\ 0 & 0 & 0 \\ 0 & 0 & 0
                \end{pmatrix}$
            }
            \drawtextnode{4}{1}{
                \tiny
                $\begin{pmatrix}
                     0 & 0 & 0 \\ 1 & 1 & 1 \\ 0 & 0 & 0
                \end{pmatrix}$
            }
            \drawtextnode{8}{2}{
                \tiny
                $\begin{pmatrix}
                     0 & 0 & 0 \\ 0 & 0 & 0 \\ 1 & 1 & 1
                \end{pmatrix}$
            }
            \drawtextnode{13.5}{3}{
                \tiny
                $\begin{pmatrix}
                     0.50 & 0.25 & 0.25 \\ 0.25 & 0.50 & 0.25 \\ 0.25 & 0.25 & 0.50
                \end{pmatrix}$
            }

            \drawtextnode{16}{0}{$[\catvariableof{\catenumerator},\catvariableof{\catenumerator-1},\dovariableof{\catenumerator}]$}
        }

    \end{frame}


    \begin{frame}{Causally augmented Markov chain}

        \input{tikz/do-variables-random-walk}

    \end{frame}


    \begin{frame}{Intervening on a position}
        \input{tikz/intervention-on-random-walk}

        {\bf Interpretation:} Restart of the random walk at time $\catenumerator$ in intervention state $\catindexof{\catenumerator}$.
    \end{frame}


    \section{Counterfactuals}

    \begin{frame}{Counterfactual worlds}

        \begin{itemize}
            \item A particular state has been observed.
            \item We reason about a hypothetical world.
        \end{itemize}

        This appears in important situation:
        \begin{itemize}
            \item Would the accident have happened had the safety protocol been respected?
            \item Would the person have developed cancer had he stopped smoking earlier?
        \end{itemize}

        \begin{center}
            \bf Interventions are not enough for these questions!
        \end{center}
    \end{frame}


    \begin{frame}{Response variables}

        Model each conditioned probability as a basis encoding of a function on the parent variables, the do-variable and the response variable
        \begin{align*}
            \selvariableof{\catenumerator}
        \end{align*}

        The response variables are shared with the counterfactual world variables.
    \end{frame}


    \begin{frame}{Random walk example}

        We model a response variable
        \begin{itemize}
            \item $\selindexof{\catenumerator}=0$ : going to the left cell
            \item $\selindexof{\catenumerator}=0$ : staying at the current cell
            \item $\selindexof{\catenumerator}=2$ : going to the right cell
        \end{itemize}

        \stantikzpic{
            \drawtextnode{-7}{0}{$\probat{\catvariableof{\catenumerator}|\catvariableof{\catenumerator-1},\selvariableof{\catenumerator}}\, = $}
            \drawtextnode{0}{0}{
                \tiny
                $\begin{pmatrix}
                     0 & 0 & 1 \\ 1 & 0 & 0 \\ 0 & 1 & 0
                \end{pmatrix}$
            }
            \drawtextnode{4}{1}{
                \tiny
                $\begin{pmatrix}
                     1 & 0 & 0 \\ 0 & 1 & 0 \\ 0 & 0 & 1
                \end{pmatrix}$
            }
            \drawtextnode{8}{2}{
                \tiny
                $\begin{pmatrix}
                     0 & 1 & 0 \\ 0 & 0 & 1 \\ 1 & 0 & 0
                \end{pmatrix}$
            }

            \drawtextnode{12}{0}{$[\catvariableof{\catenumerator},\catvariableof{\catenumerator-1},\selvariableof{\catenumerator}]$}

        }

        {\bf Effect in random walk:} Shift of the random walk.
    \end{frame}

    \begin{frame}{Counterfactual random walk}
        \begin{center}
            \bf What will be the outcome of the walk, when we do an intervention on $\catvariableof{\catenumerator}$?
        \end{center}

        \input{tikz/counterfactual-random-walk}

    \end{frame}


    \begin{frame}{Counterfactual query}

        \begin{center}
            \bf What would be the outcome of the walk, would we have done the intervention on $\catvariableof{\catenumerator}$?
        \end{center}

        \input{tikz/counterfactual-query-random-walk}

        \only<3>{
                {\bf Answer:} The walk would be shifted by the intervention. Different to the intervention query, we have a deterministic outcome!
        }

    \end{frame}
\end{document}