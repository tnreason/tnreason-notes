\documentclass{beamer}

\input{../../spec_files/course_preamble.tex}
\input{../../spec_files/course_macros.tex}

\setslidetitledate{
    Propositional Syntax and Tensor Network Decompositions
}{
    March 30, 2026
}

\begin{document}

    {\frame[plain]{\titlepage}}

    \showoutlineatsection


%    \section{Reflection: Tensors in logics}
%
%    \begin{frame}{Tensors in propositional logics}
%
%        We have seen that:
%        \begin{itemize}
%            \item Tensors are necessary to describe logics
%            \item Tensor contractions are equivalent to deciding entailment
%        \end{itemize}
%
%
%
%    \end{frame}


    \section{Motivation: Propositional syntax}

    \begin{frame}{Decomposition of formulas}
        \only<1>{
            \par
            {\bf Recall:} Tensor networks as a sparsity principle
            \begin{itemize}
                \item Knowledge base decomposition into formulas
                \item Can we exploit further sparsity in the syntax of the formulas?
            \end{itemize}
        }
        \textbf{Example:} The formula {\it "When a burglary or an earthquake occurs, either our neighbor John or Mary call."}
        has a representation by
        \begin{align*}
            \formulaat{\catvariableof{B},\catvariableof{E},\catvariableof{J},\catvariableof{M}}
            = \left(\catvariableof{B} \lor \catvariableof{E}\right) \Rightarrow \left(\catvariableof{J} \lor \catvariableof{M}\right)
        \end{align*}

        Intuitively we can decompose the
        \stantikzpic{
            \node [circle, draw, thick, fill=gray!50, minimum size = \nodeminsize] (B) at (0,-5) {};
            \drawtextnode{0}{-5}{\corelabelsize $\catvariableof{B}$}
            \node [circle, draw, thick, fill=gray!50, minimum size = \nodeminsize] (E) at (5,-5) {};
            \drawtextnode{5}{-5}{\corelabelsize $\catvariableof{E}$}

            \node [circle, draw, thick, fill=gray!50, minimum size = \nodeminsize] (Y1) at (2.5,-2) {};
            \drawtextnode{2.5}{-2}{\corelabelsize $\headvariableof{B\lor E}$}

            \draw[->-] (B) -- (2.5,-4);
            \draw[->-] (E) -- (2.5,-4);
            \draw[->-] (2.5,-4) -- (Y1);

            \node [circle, draw, thick, fill=gray!50, minimum size = \nodeminsize] (J) at (10,-5) {};
            \drawtextnode{10}{-5}{\corelabelsize $\catvariableof{J}$}
            \node [circle, draw, thick, fill=gray!50, minimum size = \nodeminsize] (M) at (15,-5) {};
            \drawtextnode{15}{-5}{\corelabelsize $\catvariableof{M}$}

            \node [circle, draw, thick, fill=gray!50, minimum size = \nodeminsize] (Y2) at (12.5,-2) {};
            \drawtextnode{12.5}{-2}{\corelabelsize $\headvariableof{J\lor M}$}

            \draw[->-] (J) -- (12.5,-4);
            \draw[->-] (M) -- (12.5,-4);
            \draw[->-] (12.5,-4)--(Y2);

            \node [circle, draw, thick, fill=gray!50, minimum size = \nodeminsize] (Y) at (7.5,1) {};
            \drawtextnode{7.5}{1}{\corelabelsize $\headvariableof{\formula}$}

            \draw[->-] (Y1) -- (7.5,-1);
            \draw[->-] (Y2) -- (7.5,-1);
            \draw[->-] (7.5,-1)--(Y);

        }

        \only<2>{
            In this lecture we:
            \begin{itemize}
                \item understand each $\headvariable$ as a \emph{hidden variable}
                \item decorate directed hyperedges by \emph{basis encodings} to find tensor network decompositions
            \end{itemize}
        }
    \end{frame}

%    \begin{frame}{Decomposition into logical connectives}
%
%        \textbf{Strategy:}
%        Store the semantics of formulas $\formulaof{1}, \formulaof{2}$ and $\formulaof{1}\exconnective\formulaof{2}$ at each decomposition of the formula.
%
%        \medskip
%
%        \begin{block}{Distributed Representation}
%            The semantics of complex formulas is then stored by a set of semantics of the used logical connectives $\exconnective\in\{\lnot, \land, \lor, \Rightarrow, \Leftrightarrow\}$.
%        \end{block}
%
%        \medskip
%    \end{frame}


    \begin{frame}{How to represent formulas as tensor networks?}

        \begin{block}{Multilinearity of tensor networks}
            Tensor networks are multilinear, that is linear in each core tensor decorating an edge.
        \end{block}

        The logical conjunction is the only connective function which is multilinear.

        \begin{block}{Non-linearity problem}
            How to represent non-linear formulas like $\lnot\catvariable$ in a tensor network?
        \end{block}

            {\bf Solution:} \\
        We develop the \emph{basis calculus} scheme instead of \emph{coordinate calculus}.
        % \end{center}

    \end{frame}


    \section{Basis calculus}

    \begin{frame}{Recall: One-hot encodings}
        One-hot encodings of a state are a tensor with a single coordinate $1$ marking the corresponding state.

        \begin{block}{One-hot encodings}
            For a state $\catindex\in[\catdim]$ of a variable $\catvariable$ the one-hot encoding is
            \stantikzpic{
                \drawsmalltensor{0}{0}{\corelabelsize $\onehotmapof{\catindexof{0}}$}
                \draw (0,-1) -- (0,-2.5) node[midway,right] {\colorlabelsize $\catvariableof{0}$};
                \drawtextnode{3}{0}{${=}$}
                \drawtextnode{11}{0}{
                    $\coloredmatrixof{0 & \cdots & 0 & 1 & 0 & \cdots& 0}{\catvariableof{0}}$
                }
                \draw[dashed] (4.5,-1) -- (4.5,-1.75) node[anchor=north] {\colorlabelsize $0$};
                \draw[dashed] (10,-1) -- (10,-1.75) node[anchor=north] {\colorlabelsize $\catindexof{0}$};
                \draw[dashed] (15.5,-1) -- (15.5,-1.75) node[anchor=north] {\colorlabelsize  $\catdimof{0}-1$};
            }
            Given a factored representation with multiple variables $\shortcatvariables$, the one-hot encoding of a state $\shortcatindices$ is the tensor product
            \begin{align}
                \onehotmapofat{\shortcatindices}{\shortcatvariables}
                = \bigotimes_{\atomenumeratorin} \onehotmapofat{\catindexof{\atomenumerator}}{\catvariableof{\catenumerator}} \, .
            \end{align}
        \end{block}

        They build an orthogonal basis of the tensor space.
    \end{frame}

    \begin{frame}{Coordinate and basis encoding}

        {\bf Coordinate encoding:} \\
        Store an information $\formulaev{\shortcatindices}$ in the coordinates of a tensor
        \begin{align*}
            \formulaat{\shortcatvariables}
            = \sum_{\shortcatindices\in[2]^{\catorder}}\formulaev{\shortcatindices} \cdot \onehotmapofat{\shortcatindices}{\shortcatvariables}
        \end{align*}

            {\bf Basis encoding:} \\
        Store an information $\formulaev{\shortcatindices}$ in a basis vector $\onehotmapofat{\formulaev{\shortcatindices}}{\headvariableof{\formula}}$ to an auxiliary variable $\headvariableof{\formula}$, that is
        \begin{align*}
            \bencodingofat{\formula}{\headvariableof{\formula},\shortcatvariables}
            = \sum_{\shortcatindices\in[2]^{\catorder}}  \onehotmapofat{\formulaev{\shortcatindices}}{\shortcatvariables} \otimes \onehotmapofat{\shortcatindices}{\shortcatvariables}
        \end{align*}

    \end{frame}


    \begin{frame}{Basis encodings}
        \begin{definition}[Basis encoding]
            The basis encodings of a formula $\formulawith \defcols \ozset^{\catorder}\rightarrow \ozset$ is the tensor
            \begin{center}
                \input{tikz/be-definition}
            \end{center}
        \end{definition}

        \textbf{Example:}
        The basis encoding of the logical disjunction $\lor$ is the tensor
        \begin{center}
            \input{tikz/be-disjunction}
        \end{center}
    \end{frame}

    \begin{frame}{Solution of the non-linearity problem}

        Basis encodings resolve the nonlinearity problem:
        Any composition of formulas can be represented as a tensor network.
        \medskip \\
        {\bf Example:} Logical negation of a formula
        \begin{align*}
            \lnot\formulaat{\shortcatvariables}
            = \contractionof{\bencodingofat{\formula}{\headvariableof{\formula},\shortcatvariables},\fbasisat{\headvariableof{\formula}}}{
                \shortcatvariables
            }
        \end{align*}
        by
        \stantikzpic{
            \drawsmalltensor{0}{4}{\corelabelsize $\fbasis$}
            \draw[->-] (0,1) -- (0,3) node[midway,right]{\colorlabelsize $\headvariableof{\exformula}$};
            \draw (-1.5,-1) rectangle (1.5,1);
            \drawtextnode{0}{0}{\corelabelsize $\bencodingof{\formula}$}
            \draw[->-] (-1,-3) -- (-1,-1);
            \drawtextnode{0}{-2}{\colorlabelsize $\shortcatvariables$}
            \draw[->-] (1,-3) -- (1,-1);
            \drawtextnode{3}{0}{${=}$}

            \begin{scope}[shift={(6,-1.5)}]
                \drawsmalltensor{0}{5.5}{\corelabelsize $\fbasis$}
                \draw (0,3.5) -- (0,4.5);
                \drawsmalltensor{0}{2.5}{\corelabelsize $\tbasis$}
                %\draw[->-] (0,1) -- (0,3) node[midway,right]{\colorlabelsize $\headvariableof{\exformula}$};
                \draw (-1.5,-1) rectangle (1.5,1);
                \drawtextnode{0}{0}{\corelabelsize ${\formula}$}
                \draw[] (-1,-2) -- (-1,-1);
                \drawtextnode{0}{-2}{\colorlabelsize $\cdots$}
                \draw[] (1,-2) -- (1,-1);
            \end{scope}

            \drawtextnode{8.5}{0}{${+}$}

            \begin{scope}[shift={(11,-1.5)}]
                \drawsmalltensor{0}{5.5}{\corelabelsize $\fbasis$}
                \draw (0,3.5) -- (0,4.5);
                \drawsmalltensor{0}{2.5}{\corelabelsize $\fbasis$}
                \draw (-1.5,-1) rectangle (1.5,1);
                \drawtextnode{0}{0}{\corelabelsize ${\lnot\formula}$}
                \draw[] (-1,-2) -- (-1,-1);
                \drawtextnode{0}{-2}{\colorlabelsize $\cdots$}
                \draw[] (1,-2) -- (1,-1);
            \end{scope}

            \drawtextnode{14}{0}{${=}$}

            \begin{scope}[shift={(17,0)}]
                \draw (-1.5,-1) rectangle (1.5,1);
                \drawtextnode{0}{0}{\corelabelsize ${\lnot\formula}$}
                \draw[] (-1,-2) -- (-1,-1);
                \drawtextnode{0}{-2}{\colorlabelsize $\cdots$}
                \draw[] (1,-2) -- (1,-1);
            \end{scope}
        }
    \end{frame}


%    \section{Basis calculus}

    \begin{frame}{Basis calculus perspective on logics}

        Boolean states are represented by one-hot encodings
        \[ \truesymbol \leftrightarrow \coloredmatrixof{0 \\ 1}{\catvariable}
        \quad \text{and} \quad \falsesymbol \leftrightarrow \coloredmatrixof{1 \\ 0}{\catvariable}\, . \]

        The truth tables of logical connectives $\exconnective\in\{\land,\lor,\Rightarrow\}$ are represented by tensors $\bencodingof{\exconnective}\in\rr^{2\times 2 \times 2}$ defined as

        Contracting with the one-hot encoding of the inputs gives the one-hot encoding of the formula satisfaction.


    \end{frame}


    \begin{frame}{Basis calculus}
        Idea of basis calculus:
        \begin{itemize}
            \item Represent input $\shortcatindices$ by one-hot encoding $\onehotmapofat{\shortcatindices}{\shortcatvariables}$
            \item Contract with the basis encoding of a function
            \item Get one-hot encoding of $\formula(\shortcatindices)$ as a result
        \end{itemize}
        For any $\shortcatindices$ it holds that:
        \stantikzpic{
            \draw[->-] (3,1) -- (3,3) node[midway,right] {\colorlabelsize $\headvariableof{\exformula}$};
            \draw (0,-1) rectangle (6,1);
            \drawtextnode{3}{0}{\corelabelsize $\bencodingof{\formula}$}
            \draw[->-] (1,-3) -- (1,-1) node[midway,right] {\colorlabelsize $\catvariableof{0}$};
            \draw[->-] (5,-3) -- (5,-1) node[midway,right] {\colorlabelsize $\catvariableof{\catorder-1}$};
            \drawsmalltensor{1}{-4}{\corelabelsize $\onehotmapof{\catindexof{0}}$}
            \drawsmalltensor{5}{-4}{\corelabelsize $\onehotmapof{\catindexof{\catorder-1}}$}

            \drawtextnode{8}{0}{${=}$}
            \draw[->-] (11,1) -- (11,3) node[midway,right] {\colorlabelsize $\headvariableof{\exformula}$};
            \draw (9.5,-1) rectangle (12.5,1);
            \drawtextnode{11}{0}{\corelabelsize $\onehotmapof{\formula(\shortcatindices)}$}

        }

    \end{frame}


    \begin{frame}{Decomposition of basis encodings}

%        \begin{block}{Representation of Composed Formulas}
%            The semantics of complex formulas are retrieved by \emph{contractions} of their connective semantics, which are summations of tensor coordinates among shared axes.
%            \begin{itemize}
%                \item Choose distributed representation to avoid contractions
%                \item Only execute those contractions required by reasoning
%            \end{itemize}
%        \end{block}

        Example of a decomposition:
        \begin{center}
            \input{./tikz/be-decomposition}
        \end{center}

    \end{frame}


    \begin{frame}{Formula evaluation by message passing of one-hot encodings}

        \only<1-5>{
            \emph{Basis Calculus:} Computation by basis vectors
            \begin{itemize}
                \item Input states are prepared by their one-hot encodings
                \item Contractions with the basis encodings result in the one-hot encoding of the output
            \end{itemize}
            Advantage: Contractions can performed by local contractions.
        }

        \begin{center}
            \input{./tikz/basis-calculus}
        \end{center}

        \only<6>{
            This is a first example of a \emph{message passing scheme:}
            \begin{itemize}
                \item "Messages" are computed by contractions of sub-networks (two blue contractions, one red contraction)
                \item "Passing" of previous messages (blue contractions) to compute new messages (red contraction)
            \end{itemize}
        }
    \end{frame}





    \begin{frame}{\DecompositionSparsity{}}

        \begin{theorem}
            The basis encoding of a composed formula is the contraction of the basis encodings to its connectives.
        \end{theorem}
        \textbf{Proof:} When contracting the decomposition with one-hot encodings of any $\shortcatindices\in[2]^{\catorder}$, we get by basis calculus the one-hot encodings of the composition formulas, and finally the one-hot encoding of the whole formula.
        This coincides with contracting the full basis encoding since
        \begin{align*}
            \contractionof{\bencodingofat{\formula}{\headvariableof{\exformula},\shortcatvariables},\onehotmapofat{\shortcatindices}{\shortcatvariables}}{
                \headvariableof{\exformula}
            }
            = \onehotmapofat{\formulaat{\indexedshortcatvariables}}{\headvariableof{\formula}} \, .
        \end{align*}
    \end{frame}

%    \section{Revisiting entailment}
%
%    \begin{frame}{Logical Inference: Entailment}
%
%        The semantics of logics enable the calculation of logical consequences, framed logical inference.
%
%        \begin{definition}[Entailment]
%            A propositional formula $\exformula$ is entailed by a formula $\kb$, if for each state $\atomindices$ with $\kb(\atomindices)=1$ we have $\exformula(\atomindices)=1$.
%            We denote in this case
%            \[ \kb \models \exformula \, . \]
%        \end{definition}
%
%        Approaches to automate the decision of entailment:
%        \begin{itemize}
%            \item Proof-theoretic approaches: Inference rules such as Modus ponens
%            \item Model-theoretic approaches: Check whether $\kb\land\lnot\exformula$ has a model.
%        \end{itemize}
%
%
%    \end{frame}
%
%
%
%    \begin{frame}{Revisiting entailment}
%
%        \begin{theorem}[Contraction Criterion for Entailment]
%            We have $\kb\models\exformula$ (respectively $\kb\models\lnot\exformula$) if and only if
%            \[ \contractionof{\kb,\bencodingof{\exformula}}{\catvariableof{\exformula}} \parallel \onehotmapof{1} \quad \text{(respectively  } \contractionof{\kb,\bencodingof{\exformula}}{\catvariableof{\exformula}} \parallel \onehotmapof{0} \text{)} \, .\]
%        \end{theorem}
%
%        We depict this condition by
%        \begin{center}
%            \input{./tikz/entailment_check.tex}
%        \end{center}
%
%    \end{frame}


    \begin{frame}{Advantages of the Network Decomposition}

        \begin{block}{Model-theoretic Semantics by Tensor Networks}
            \begin{itemize}
                \item \textbf{Tensors encode the models} of propositional formulas by their nonzero coordinates
                \item \textbf{Networks eliminate redundancies} when representing similar formulas
            \end{itemize}
            %Contractions with cores $\bencodingof{\exconnective}$ can be designed for various reasoning tasks.
        \end{block}

        \medskip

        This is an implementation of the \textbf{neural paradigm}, resulting in
        \begin{itemize}
            \item Effective representation: Latent variables by logical formulas
            \item Differential parametrization: Multilinearity with respect to each core tensor
        \end{itemize}

    \end{frame}


    \section{Implementations in \tnreason{}}

    \begin{frame}{Implementations in \tnreason{}: \\
    Subpackage \sprepresentation{}}

        The subpackage \sprepresentation{} is dedicated to the tensor encoding schemes such as basis encodings.

        \begin{center}
            \begin{tikzpicture}[scale=0.27]
                \drawtnreasonarchitecture{}
                \draw[blue] (-10,4) rectangle (0,8);
                \node [anchor=center,blue] at (-5,6) {\sprepresentation{}};

            \end{tikzpicture}
        \end{center}

    \end{frame}

    \begin{frame}[fragile]{Subpackage \sprepresentation{}: Basis encodings}
        Creation of a basis encoding $\bencodingofat{\exformula}{\headvariableof{\exformula},\shortcatvariables}$:
        \begin{minted}{python}
    from tnreason import representation
    basisEncodedCore = representation.create_basis_encoding_from_lambda(
        inShape, outShape, inColors, outColors, function)
        \end{minted}
        \begin{itemize}
            \item inShape, outShape, inColors, outColors: specifying the leg dimension and the names of the incoming variables ($\shortcatvariables$) and the outgoing variables ($\headvariableof{\exformula}$)
            \item function: computes the state of the outgoing variables given the state of the incoming variables
        \end{itemize}

    \end{frame}

    \begin{frame}{Implementations in \tnreason: \\
    Subpackage \spapplication{}}

        The subpackage \spapplication{} provides high-level models
        \begin{itemize}
            \item Interpretation of propositional syntax
            \item Training on empirical data (later)
        \end{itemize}

        \begin{center}
            \begin{tikzpicture}[scale=0.27]
                \drawtnreasonarchitecture{}
                \draw[blue] (-10,10) rectangle (10,14);
                \node [anchor=center,blue] at (0,12) {\corelabelsize \spapplication{}};
            \end{tikzpicture}
        \end{center}

    \end{frame}

%    \begin{frame}{Representation of Propositional Formulas}
%
%        Propositional formulas have three representations specified in the subpackage \sprepresentation{}:
%        \begin{itemize}
%            \item \emph{Syntax} of formulas $\exformula$ is stored in a script language $\scriptrepof{\exformula}$ based on nested lists of strings
%            \item \emph{Semantics} of formulas is stored by tensor networks of connective cores
%            \item Random variable representing the formula satisfaction
%        \end{itemize}
%
%    \end{frame}



    \begin{frame}[fragile]{Representation of Syntax}

        In \tnreason, propositional syntax is represented by \emph{nested lists of strings}, for example:
        \begin{minted}{python}
    ["and", ["not", "Rained"], ["imp", "Rained", "Wet"]]
        \end{minted}
        \medskip

        Connectives $\exconnective$ get assigned a string $\scriptrepof{\exconnective}$:
        \begin{center}
            \begin{tikzpicture}
                \node [anchor=center] at (0,0) {
                    \begin{tabular}{|c|c|}
                        \hline
                        \textbf{Unary connective $\exconnective$} & \textbf{$\scriptrepof{\exconnective}$} \\
                        \hline
                        $\lnot$                                   & \stringof{not}                         \\
                        \hline
                        $()$                                      & \stringof{id}                          \\
                        \hline
                    \end{tabular}};
                \node [anchor=center] at (5,0) {
                    \begin{tabular}{|c|c|}
                        \hline
                        \textbf{Binary connective $\exconnective$} & \textbf{$\scriptrepof{\exconnective}$} \\
                        \hline
                        $\land$                                    & \stringof{and}                         \\
                        \hline
                        $\lor$                                     & \stringof{or}                          \\
                        \hline
                        $\Rightarrow$                              & \stringof{imp}                         \\
                        \hline
                        $\oplus$                                   & \stringof{xor}                         \\
                        \hline
                        $\Leftrightarrow$                          & \stringof{eq}                          \\
                        \hline
                    \end{tabular}};
            \end{tikzpicture}
        \end{center}

        Any other string is interpreted by an atomic formula (a so-called propositional symbol).

    \end{frame}

    \begin{frame}{Representation of Syntax}
        Having strings representing
        \begin{itemize}
            \item Connectives (by defined representations)
            \item Atomic Formulas (all other strings)
        \end{itemize}
        we represent formulas $\formulaof{1}\exconnective\formulaof{2}$
        \begin{centeredscript}
            $\scriptrepof{\formulaof{1}\exconnective\formulaof{2}}$ = [$\scriptrepof{\exconnective}$, $\scriptrepof{\formulaof{1}}$, $\scriptrepof{\formulaof{2}}$]
        \end{centeredscript}
        where we apply the conventions
        \begin{itemize}
            \item Connectives are at the 0th position in each list
            \item Further entries are either atoms as strings or encoded formulas itself
        \end{itemize}
    \end{frame}

    \begin{frame}{Examples of representations}
        Atomic variable $\var{Rained}$ by
        \begin{centeredscript}
            $\scriptrepof{\var{Rained}}$ = \pyinline{"Rained"}
        \end{centeredscript}
        Negative literal $\lnot\var{Rained}$ by
        \begin{centeredscript}
            $\scriptrepof{\lnot\var{Rained}}$ = \pyinline{["not","Rained"]}
        \end{centeredscript}
        Horn clause $\left(\var{Rained}\Rightarrow\var{Wet}\right)$ by
        \begin{centeredscript}
            $\scriptrepof{\var{Rained}\Rightarrow\var{Wet}}$ = \pyinline{["imp","Rained","Wet"]}
        \end{centeredscript}
        Knowledge Base
        $\kb=(\lnot\var{Rained})\land(\var{Rained}\Rightarrow\var{Wet})$ by
        \begin{centeredscript}
            $\scriptrepof{\kb}$ = \pyinline{["and", ["not", "Rained"], ["imp", "Rained", "Wet"]]}
        \end{centeredscript}
    \end{frame}


    \begin{frame}[fragile]{Basis encoding decompositions}
        The recursive structure of the nested lists \pyinline{expression}=$\scriptrepof{\exformula}$ is exploited in finding tensor network representations of $\exformula$.
        \begin{minted}{python}
    from tnreason.representation import formula_to_cores as ftc
    coreDict = ftc.create_formula_computation_cores(expression)
        \end{minted}
        The function creates a tensor networks of basis encodings to connectives by:
        \begin{itemize}
            \item If $\scriptrepof{\exformula}$ a list, add $\bencodingof{\scriptrepof{\exformula}[0]}$ and add tensors from recursive calls to the subformulas $\scriptrepof{\exformula}[1:]$
            \item Return empty dictionary, when $\scriptrepof{\exformula}$ is a string (then an atom)
        \end{itemize}
    \end{frame}


\end{document}













